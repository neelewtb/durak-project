[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "durak"
version = "0.1.0"
description = "interactive durak-cardgame"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
durak = "durak.__main__:main"